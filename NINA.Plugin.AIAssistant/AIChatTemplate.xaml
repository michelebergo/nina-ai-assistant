<ResourceDictionary
    x:Class="NINA.Plugin.AIAssistant.AIChatTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:NINA.Plugin.AIAssistant">

    <!-- AI Chat Dockable Panel - applied automatically by DataType -->
    <DataTemplate DataType="{x:Type local:AIChatVM}">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="{StaticResource SecondaryBackgroundBrush}" 
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Padding="10,8" 
                    CornerRadius="5"
                    Margin="0,0,0,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="ai assistant" 
                                   FontSize="14" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryBrush}"/>
                        <TextBlock Text="{Binding StatusMessage}" 
                                   FontSize="11" 
                                   Opacity="0.7"
                                   Foreground="{StaticResource PrimaryBrush}"/>
                    </StackPanel>
                    <Button Grid.Column="1" 
                            Content="Clear" 
                            Command="{Binding ClearChatCommand}"
                            Padding="10,5"
                            VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Chat Messages -->
            <Border Grid.Row="1" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1" 
                    CornerRadius="5"
                    Background="{StaticResource BackgroundBrush}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Messages}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:ChatMessage}">
                                <Border Margin="5,3" Padding="10,8" CornerRadius="8"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
                                            <Setter Property="HorizontalAlignment" Value="Left"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsError}" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource NotificationErrorBrush}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource NotificationErrorBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel MaxWidth="500">
                                        <TextBlock Text="{Binding Content}" 
                                                   TextWrapping="Wrap" 
                                                   Foreground="{StaticResource PrimaryBrush}"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}'}" 
                                                   FontSize="10" 
                                                   Opacity="0.5"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   HorizontalAlignment="Right"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Input Area -->
            <Grid Grid.Row="2" Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" 
                         Text="{Binding UserMessage, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="False"
                         VerticalContentAlignment="Center"
                         Padding="10,8"
                         Margin="0,0,5,0"
                         Background="{StaticResource SecondaryBackgroundBrush}"
                         Foreground="{StaticResource PrimaryBrush}"
                         BorderBrush="{StaticResource BorderBrush}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding SendMessageCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1" 
                        Content="Send" 
                        Command="{Binding SendMessageCommand}"
                        Padding="15,8"
                        MinWidth="60"/>
            </Grid>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
